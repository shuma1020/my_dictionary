ja:
  activerecord:
    models:
        post: 投稿 # view側： User.model_name.human => "ユーザ" / t("activerecord.models.user")と同じ
    attributes:
        post:
          id: ID
          title: タイトル # view側： User.human_attribute_name :name => "名前" /　t("activerecord.attributes.user.name")と同じ
          body: 内容
          status: 公開状況
          posted_at: 投稿日
          reason: メモした経緯
          rank: 重要度
          summary: 要約